 /**
 * And gate:
 * if ((a == 1) and (b == 1)) out = 1, else out = 0 
 */

 CHIP And {
    IN a, b;
    OUT out;
    
    PARTS:
    Nand(a=a, b=b, out=nandAB);
    Nand(a=nandAB, b=nandAB, out=out);
}
